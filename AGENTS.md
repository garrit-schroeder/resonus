# Expo HAS CHANGED

This project is on SDK 57 (`expo` in `package.json` is the authority). Read the
exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing
any code, and update this line when the SDK is bumped.
